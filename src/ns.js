// temporary namespace hack file

if (!window.multigraph) { window.multigraph = {}; }
if (!window.multigraph.Axis) { window.multigraph.Axis = {}; }
if (!window.multigraph.Legend) { window.multigraph.Legend = {}; }
if (!window.multigraph.Plot) { window.multigraph.Plot = {}; }
if (!window.multigraph.core) { window.multigraph.core = {}; }
